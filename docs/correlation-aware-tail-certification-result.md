# Correlation-Aware Tail Certification Lab — result

Parent: `761c2dd6505f99a953f143a43221393370feebd0`.
Branch: `correlation-aware-tail-certification-lab`.

```text
decision: stop_correlation_aware_certificate_inconclusive
subclass: CERTIFICATION INCONCLUSIVE
selected_precision: null
promotion: NO_PROMOTION TO AUTHORITATIVE WORLD DYNAMICS
```

**Correlation preservation resolves the previous first-step failure, but the
registered implementation exhausts its verifier memory budget before completing
any full tail. No B96 physical-budget violation or structural defect is established.**

Publication identity, CI outcomes, archive digests and fresh-download verification
belong to the immutable seal and external publication receipts. This document
does not predeclare those operational steps successful.

## Frozen science and parent reproduction

Main was fast-forwarded to the accepted parent without rewriting its history.
All accepted mechanics, state arithmetic, candidate precisions, KDK order, force
domain, physical budgets, topology, H_force and reference coordinates are
unchanged. No B256 trajectory served as truth. No candidate residual, corrective
force, energy reservoir or new physical state was introduced.

The unchanged parent verifier reproduced its three anchor failures, rounding
counterexamples and signed cancellation audit. All ten independent-box full
pilots still stop at step 1, second kick; 60/90 exact-checkpoint blocks complete
with 1,320 withheld checks. The signed-record audit checks 4,018 samples. This
is not a rerun of the optional full exact-prefix trajectory replay.

## What the new enclosure actually demonstrates

The shared affine implementation completed every registered withheld block:
both internal and boosted scenarios, all five levels, starts 0/8/32 and lengths
1/4/16. **90/90 blocks and 1,890 joint stage checks passed.** None needed branching.

Containment was checked using a single common exact assignment to the retained
noise symbols after generation, against separately evolved exact-rational
packet states. The generator did not read those assignments or comparator
states. Checking unrelated coordinate intervals would have been weaker.

A matched control isolates the old failure rather than attributing it to the
new arithmetic generally. At the first second-kick input, all ten cases have
relation 5, x-component enclosed as exactly `[0,0]` under shared subtraction.
Using independent subtraction on the **same rounded affine input forms**
produces a nonzero interval straddling zero and unresolved binary64 conversion.
Thus losing the shared dependency is sufficient to reintroduce the old failure.
This does not prove that every later uncertainty can be treated efficiently.

The independent matched replay also checks 30 conditioned affine stage images
using sparse matrix rows and explicit substitution of rounding allowances.
Independent binary64-cell checks use integer bit decoding and exact rational
RN-even preimages, including ties and signed zero.

## Full-horizon pilots

All ten cases were attempted separately with B (shared affine, no splitting)
and C (shared affine plus deterministic splitting), from exact time-zero states.
The full generators read no later exact comparator and no B96/B192/B256 trace.

Every primary pilot stopped at the preregistered **2 GiB process address-space
limit**, before the 4,096-active-symbol or cell limits. No ambiguous conversion
triggered a branch before the memory stop. Therefore the registered full runs
do not demonstrate whether C's branching would help at a later ambiguity.

The table gives the C primary run. B stopped at the same steps and stages.
“Complete prior time” excludes the interrupted step; it is not a certified
physical-error horizon.

| Scenario | Level | Stopping step / stage | Complete prior time (s) | Symbols created |
| --- | ---: | --- | ---: | ---: |
| Internal | 0 | 77 / second kick | 19/4 | 2762 |
| Internal | 1 | 78 / first kick | 77/32 | 2772 |
| Internal | 2 | 78 / first kick | 77/64 | 2777 |
| Internal | 3 | 77 / drift | 19/32 | 2748 |
| Internal | 4 | 76 / second kick | 75/256 | 2724 |
| Boosted | 0 | 79 / first kick | 39/8 | 2814 |
| Boosted | 1 | 79 / first kick | 39/16 | 2813 |
| Boosted | 2 | 79 / first kick | 39/32 | 2812 |
| Boosted | 3 | 79 / first kick | 39/64 | 2811 |
| Boosted | 4 | 79 / first kick | 39/128 | 2808 |

All required complete-horizon quantities remain **UNCERTIFIED**: position and
momentum representation errors, representation energy and its signed slope,
momentum/angular/centrality residual bounds, boosted relative-state errors, and
full-horizon safe-domain membership. Neither a physical-budget violation nor
a domain violation was established. Conditional B128/B192 selection was not
activated, because there was no rigorous B96 budget violation.

## Resource diagnosis and limits of interpretation

The verifier retains historical affine-expression cache entries and exact
rounding-provenance definitions. As more noise symbols enter subsequent dense
forms, these retained histories grow much faster than the current phase vector.
The observed resource stop therefore concerns this implementation, not an
intrinsic lower bound on correlation-aware verification or bounded mechanics.

No generator was dropped to fit the budget. No memory ceiling, precision,
timestep, candidate state, physical budget or conversion outcome was relaxed.
The closed-bisection scheme is soundly covering but is not claimed to resolve
every rounding boundary within finitely many cells. It was not exercised by
these full trajectories before their memory stops.

Polynomial observer primitives retain signed common affine coefficients and
enclose quadratic remainders outward. Their known-answer tests pass. They were
not used to manufacture a full-tail energy or slope certificate after the state
enclosure stopped. A constant shared-noise slope fixture initially exposed
unnecessary pre-summation rounding; combining signed coefficients before
rounding corrected that unit fixture, without changing any physical budget.

## Independent repeat and determinism boundary

An independent repeat of all 20 full pilots reproduced the stopping step,
stage and resource disposition in **20/20** cases. **18/20 raw execution records
were byte-identical.** In boosted L2, the B record's symbol count changed from
2811 to 2812 and C's changed from 2812 to 2811 at memory exhaustion. Both
primary and repeat records are preserved, unnormalized.

Consequently, this lab does **not** claim a bit-identical full execution twin.
The variation is in allocator-stop bookkeeping, not evidence of a changed MLS
trajectory, but the strict raw-record identity condition was not met.
Deterministic archive materialization from the frozen recorded inventory is a
separate claim, to be checked by independently constructing both archives.

## Proof and validation scope

The nine new Lean statements cover image inclusion, shared subtraction, closed
child coverage, fixed-scalar KDK algebra, branch-union induction, signed affine
sums, recentering, budget slack, and signed-numerator envelopes. They do not
prove the executable floating-point implementation or assume a candidate trace
is exact. The pinned local build completed 2,141 jobs and the trust scan passed.

The new exact-arithmetic/guard/audit suite has 33 tests, alongside the ten
unchanged parent controls. The release verifier additionally replays the ten
matched dependency controls and their independent affine-image checks. By
default it replays all 90 new withheld blocks and requires byte-identical
results; `--records-only` explicitly skips that expensive new replay.

Normal fresh verification does not rerun the earlier full exact-prefix
trajectories or the memory-exhaustion pilots. It authenticates their preserved
records and reports these scope limits explicitly. The complete accepted parent
bundle is embedded, and all inherited source files remain byte-identical except
the additive formal axiom report.

## Preserved attempts and disposition

An early full-horizon prototype was launched before the complete withheld
inventory finished. It was explicitly interrupted with exit 130, produced no
result, and is preserved as ineligible exploratory work. It is not counted among
the 20 primary or 20 repeated full pilots. The final launcher checks the entire
withheld inventory before starting any full-horizon subprocess.

This is a completed registered **inconclusive experiment**, not a successful
B96 certificate. The full-budget and strict raw-execution-identity conditions
remain unmet. The narrow causal result is useful: independent boxes lost a
dependency that shared affine forms retain. The remaining certificate is blocked
by the registered verifier's resource use. Do not interpret that as rejection
of bounded phase state, alter the frozen mechanics, or promote World dynamics.
