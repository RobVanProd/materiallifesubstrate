# Explicit Fractional Phase-State Lab contract

## Scope

This bounded experiment starts only from the accepted
`phase-space-time-corefinement-lab-evidence-v1` source commit
`6dfaf29821ded7e1349358c671b52e73f345c26a`. It asks whether the generic
kick/drift lattice obstruction disappears when packet position and momentum
carry explicit exact fractional state.

It is not an integrator comparison and does not install mechanics in `World`.
Contact, fracture, gravity, chemistry, adaptive precision, time integration
outside this lab, and every production or authoritative dynamics claim remain
out of scope. Every result is `NO_PROMOTION`.

## Frozen mechanics

The candidate preserves the accepted topology, reference state, symmetric
`H_force`, local collective energy, central force law, cancellation-resistant
binary64 relation geometry, `r/l0 >= 2^-24` domain, KDK stage order, physical
scenarios, horizons, and timestep hierarchy.

The authoritative experimental packet state is

```text
x = Lq (X + xi),   p = Pq (P + pi),
X,P in signed64,   xi,pi in Q.
```

Rationals are reduced, have positive denominators, and use canonical residuals
in `[-1/2,1/2)`. There is no fixed denominator, truncation, snapping, remainder
ledger, hidden energy, or persistent floating state. The full rational state
is hashed, checkpointed, replayed, permuted, and consumed by every phase-state
observer.

## Candidate maps

Relation subtraction occurs exactly before the relative vector is converted to
binary64 for Path B. The accepted binary64 length and conjugate are reconstructed
from their exact bit patterns. Multiplying the resulting exact rational scalar
by the complete exact relation vector makes every endpoint impulse exactly
central in authoritative fractional geometry.

Drift evaluates `x_next = x + (p/m) dt` in exact rational arithmetic. The only
post-operation normalization is canonical integer/residual carry. Every chord
is checked against the accepted force domain before commit. Failure returns the
complete prior state atomically.

## Gates

The parent raw evidence is pinned by exact file hashes. Candidate and first-order
control paths use five timestep halvings for K4 breathing, K4 internal velocity,
and octahedron deformation. The KDK path must show three consecutive orders in
`[1.6,2.4]`; the first-order control must remain distinguishable.

At every stage, exact rational total momentum and orbital angular momentum must
equal their initial values. Signed-time recovery, canonical checkpoint replay,
atomic domain rejection, translation, proper lattice rotation, packet
permutation, and the common-velocity boost are mandatory.

The exact-state ceilings are frozen at 262,144 maximum component bits, 131,072
median component bits per state, and 8,388,608 canonical checkpoint bytes. The
first crossing state is retained exactly and then the affected trajectory
stops. No state is rounded to pass the gate.

The independent oracle uses 110 decimal digits and the exact exported binary64
bit patterns. Twin raw outputs must be byte-identical. GCC, Clang, MSVC, Python,
Lean, semantic mutations, outer-seal mutations, immutable tag CI, and a fresh
public download must all pass before completion.

## Decision order

The first applicable decision is:

1. `stop_inconclusive_or_wrong_parent`;
2. stop and correct a false reciprocal-obstruction interpretation;
3. `reject_fractional_phase_state_accounting`;
4. `reject_fractional_phase_state_as_quantization_cure`;
5. `fractional_phase_state_restores_dynamics_but_bounded_representation_unresolved`;
6. `retain_explicit_fractional_phase_state_for_research`.

Every decision remains `NO_PROMOTION TO AUTHORITATIVE WORLD DYNAMICS`.
