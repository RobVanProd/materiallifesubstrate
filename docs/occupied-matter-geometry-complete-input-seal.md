# Complete pre-data input seal

The authorized narrow Cartesian structural-validity certificate has been
established and independently verified. No further protocol amendment was
needed. This receipt opens the existing authorized data gate; it is **not** a
candidate result or a completed geometry lab. **NO_PROMOTION remains.**

- Generator/seal source: `cf90ef0c25419c3f41dda67b99224747d9f9009a`.
- Accepted mechanics parent: `639ab635769a7d244aa255281acbabad78c27057`.
- Input root SHA-256:
  `8ec8ba42956f7664c66de26ce2f760be650cba76ef7fe0820d92396206e84516`.
- Candidate-manifest SHA-256 (unchanged numerical input root):
  `5e089e33a1dd0660dcc9d94f432a0b054be681de917173d53a4172f1f46c957a`.
- Stored size including input root and manifests: **8,535,385,279 bytes**.
- Remaining under the shared 8 GiB ceiling: **54,549,313 bytes**.
- Registered inventory: **1,155 runs; 2,310 I1/I2 views**.
- Candidate evaluations at sealing: **zero**.

The sealed directory is `build/occupied-geometry-input-staging-v3`; the existing
directory name is retained. `input-root-seal.json` is the root record. Do not
modify its inputs/manifests after data. Earlier staging attempts remain intact.
Any later candidate evidence and additional source still count toward the same
8 GiB ceiling; the remaining allowance is not a guaranteed final-package fit.

## Cartesian validity exception closed

The independent template checker verifies six positive unit-determinant chain
simplices, their exact barycentric forms, all 15 strict-order incompatibilities,
six canceling internal face pairs, twelve exterior triangles, and matching
opposite neighbor faces along all three axes. The accompanying proof explains
global Cartesian replication and invertible-affine preservation.

All ten freshly copied cube/slab levels passed the independent full-table
replay under their individual memory/time limits. The certificate binds all
330 applicable transformed/order/ID rows to the exact input root. Six mutations
(root, vertex, grid, transform, decoded stream and illicit geometry answer)
were rejected. The full pinned Lean build passed with **2,146 jobs**.

Only the authenticated structural validity premise is exempted at runtime.
Union, buried-boundary filtering, overlap, normals, distances, Hausdorff and CCD
remain ordinary charged geometry work. Sphere, U and moving-complex validity
remain outside this exception. C's predeclared finest cube/slab resource rows
remain unchanged.

The complete root also binds the independent sphere diameter/radial-support,
injectivity and sample/weight joins, exact weight replay, prescribed motions,
query inventories, oracle constants/controls, global/native decoding and
order/relabel audits. Candidate and oracle bytes remain physically separate.

## Preserved seal attempt

The earlier `3a3ea80` attempt stopped atomically on a missing receipt alias;
no root was written. `cf90ef0` preserved all source-to-blob aliases when receipt
bytes deduplicate. Neither numerical inputs nor governing scientific rules
changed. See the separate input-seal-attempts record.

Main, accepted mechanics and World ownership are untouched. No tag, release,
merge or final scientific disposition is asserted by this input milestone.
