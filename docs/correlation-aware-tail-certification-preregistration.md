# Correlation-Aware Tail Certification Lab — preregistration

Parent: `761c2dd6505f99a953f143a43221393370feebd0`.
Branch: `correlation-aware-tail-certification-lab`.
Status: preregistered implementation, no new trajectory disposition.
Promotion: **NO_PROMOTION TO AUTHORITATIVE WORLD DYNAMICS**.

## Frozen boundary and parent control

Preserve the accepted parent, its inconclusive disposition, source, tag and
release. Candidate phase arithmetic, binary64 force operation graph, units,
topology, H_force, reference state, KDK, precision inventory and physical
budgets remain unchanged. B256 is never truth. The target is the exact-rational
KDK map INCLUDING its registered binary64 geometry conversions, not the smooth
ODE. The ODE and representation errors are separate questions.

Before candidate implementation, rerun the unchanged parent record verifier:
three exact anchor failures, exact rounding controls, L3 cancellation audit,
ten whole-horizon pilots failing at step 1 second kick, 60/90 completed blocks,
1,320 withheld checks, no full-tail certificate. This is record arithmetic and
box replay, not a new full-prefix dynamics replay. Authenticate the input
bundle against its sealed manifest and source. Any discrepancy stops this lab
as `stop_inconclusive_or_wrong_parent`.

## Correlated state and propagation

Candidate B uses shared affine forms c + sum(G_j eta_j), with dyadic centers
and coefficients and identified common symbols. Residual rounding allowances
are explicit fresh noise symbols (equivalent to outward residual boxes until
their subsequent shared use). A symbol must retain its identity across all
packet coordinates and endpoint accumulations. Exact zero cancellations are
performed before rounding. Centers and coefficients use the inherited
512-bit outward dyadic arithmetic; every discarded coefficient or center part
has an explicit outward allowance. No verifier precision increase after data.

Within a certified binary64 conversion cell, relation coefficients are fixed
exact rationals from frozen scalar bit patterns. Apply the resulting affine
central kick and linear drift to the shared forms. Coefficients involving
non-dyadic division must retain outward slack. Safe-domain tests enclose both
states and every full drift chord at r/l0 >= 2^-24. Failure to prove domain
safety is inconclusive, not a domain violation.

Candidate C partitions an ambiguous correlated domain. Use stable relation
order then axis to choose the first ambiguous conversion, largest absolute
generator contribution to choose a split, stable numeric generator ID to
break ties. Bisect its current interval exactly; retain BOTH children,
including a closed shared midpoint. Retry the interrupted stage from its
unmodified incoming state. No pruning using a nominal trajectory, no merge of
different force outcomes, and no zero-radius checkpoint restart. If rounding
ambiguity has no splittable generator, report that explicitly. Coverage and
round-to-nearest/ties-to-even membership are exact rational obligations.

B96 may supply a convenient nominal center only with a proved recentering
identity; it is never an exact starting point. Exact initialization is the
default. Replacing the nominal center must preserve the represented set.

## Frozen resource limits and inventory

Before any new candidate trajectory data, freeze these limits per trajectory:

- 256 live cells; 4,096 cumulative cells (including roots and children).
- 4,096 active shared symbols per cell; no symbol deletion to fit the limit.
- 900 seconds wall time; 2 GiB process address-space ceiling.
- All arithmetic remains 512-bit outward, including verifier coefficient slack.

Wall time and memory are operational guards, not scientific passing thresholds.
Deterministic cell/symbol limits identify reproducible algorithmic boundaries;
if an operational guard triggers first, preserve it without converting it into
a pass. A limit must not cause a trajectory subset to be silently omitted.

Primary inventory: B96, internal L1,L2,L3,L4,L0 then boosted L0..4, each for
the frozen 16 seconds and timestep. Run B (no branching) and C (branching)
separately. B192 is optional diagnostic only and cannot replace an inconclusive
B96. Only after a rigorous B96 budget violation may the same certificate test
B128 and then B192 conditionally; no new precision is introduced.

## Validation before tails

Unit checks independently verify affine transformations, outward slack,
generator identities, child union coverage, binary64 tie cells and signed
weighted sums. Withhold exact comparator states from generation and check
containment afterward at every kick and drift for the inherited checkpoint
inventory: starts 0,8,32, lengths 1,4,16, both scenarios and five levels.
Exact checkpoints justify their own starting points but never repair a failed
time-zero continuation. Branch-tree checks must establish union containment,
not require that every child contain the exact point.

Mutations must detect dropped shared symbols, independent subtraction in place
of shared subtraction, inward coefficient rounding, non-nominal branch pruning,
incompatible-cell merging, assumed B256 outcomes, radius resets, resource
exhaustion relabeled as passing, and absolute values inserted before signed
slope accumulation. Recenter tests must change the nominal center without
changing the target set.

## Physical certificate, not an error-ratio qualification

Copy no fitted multipliers into acceptance. Retain the exact parent budgets
from `bounded_fractional_phase_state_oracle.py`: position, momentum, angular /
centrality, energy and slope budgets and all corresponding unit definitions.
Certification must cover every required sample and every branch over the full
horizon for position/momentum representation errors, energy error and slope,
total momentum, orbital angular momentum, centrality, boosted relative state,
and safe-domain membership. Stored candidate residuals remain separately
measurable; they cannot substitute for an uncomputed target enclosure.

Bound the signed least-squares numerator S = sum((t-mean(t))*deltaE) directly
with common dependencies when possible, divide by the exact positive time
denominator. An absolute-weight sample envelope is permitted only as a labeled
weaker fallback, never as a causal explanation of cancellation. No uncertainty
reset between blocks. If state propagation itself fails, downstream quantities
are explicitly uncertified rather than inferred from B256 or prefixes.

## Proof, decisions, and completion

Lean covers affine image inclusion, shared subtraction, child coverage,
fixed-scalar KDK composition, branch-tree enclosure induction, signed weighted
sums, and enclosure plus budget slack implying error bounds. It does not prove
the floating implementation, and no theorem assumes the candidate is exact.

Decision priority: wrong parent; `stop_certificate_unsound` for containment or
mutation failures; `stop_correlation_aware_certificate_inconclusive` for sound
but incomplete enclosure; `b96_bounded_tail_budget_exceeded` only for a proved
violation; `retain_b96_bounded_phase_state_for_research` with selected precision
96 only if every applicable full-tail gate is certified. Otherwise selection
is null. Every outcome remains NO_PROMOTION.

Completion requires parent reproduction, noncircular withheld validation,
independent arithmetic/mutations, the full inventory or explicit reproducible
certificate boundary, Lean and cross-platform gates, deterministic twins,
publication and fresh-download verification. Preserve failed attempts. Stop
after sealing; do not modify World mechanics.
