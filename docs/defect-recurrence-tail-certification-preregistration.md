# Defect-Recurrence Tail Certification Lab — preregistration

Parent: `171066fc15d6096739c7aeb2e99da7313f447d3f`.
Branch: `defect-recurrence-tail-certification-lab`.
NO_PROMOTION TO AUTHORITATIVE WORLD DYNAMICS; selected precision remains null
unless every required complete-horizon budget is certified.

## Order and frozen boundary

The protected main fast-forward preserves the accepted parent. Before the full
verifier is implemented, the exact Lean defect identity, enclosure step,
sequence induction, signed accumulator and outward-slack lemmas are compiled.
The candidate is arbitrary in these statements; no exactness is assumed.

All inherited files remain unchanged except additive theorem reporting.
B96 state arithmetic, force operation graph, Path B, H_force, topology,
reference, units, KDK, exponent range, trajectories, domain r/l0 >= 2^-24,
and physical budgets are immutable. B256 is never truth. No new candidate
precision, correction state, branching, or larger verifier precision.

## Fixed-dimensional method

For each stage reconstruct candidate input/output from canonical B96 wire
bytes with an independent decoder. Authenticate initial and complete-step
hashes against the sealed corpus; stage reproduction uses the unchanged
candidate implementation, separately from the certificate calculation.
Compute d = A*candidate + b - candidateNext exactly as rationals. Apply the
conditioned sparse affine operator to the CURRENT error intervals, add d
exactly, and round each output endpoint outward to 512 significand bits.
No half-ULP symbol history or historical expression cache is retained.

The persistent numerical state has 6*N intervals, each with two 512-bit dyadic
endpoints, and a fixed inventory of observer accumulators. Endpoint exponents
must lie in [-16384,16384]; exceeding this pre-data verifier-only bound stops
inconclusively. Scratch rational arithmetic is restricted to one stage and
discarded afterward. Logical phase/error/matrix slots depend only on N and
relation count, not step count. No historical states feed future propagation.

Certify exact candidate relative offsets plus (error_j-error_i), including
the exact SI conversion, in exact binary64 RN-even preimage cells. The nominal
bit pattern is only a proposal until the entire range is included. A failure
stops; do not branch, shrink the radius, or assume a symmetry not represented
by this component-wise enclosure. This audit explicitly tests whether an
exact-zero relation component can remain unresolved despite small error balls.

## Controls and inventory

Authenticate both accepted certificate bundles and their source/tag identities.
Reproduce the independent-box decisive controls and use the accepted affine
90-block replay receipts plus authenticated records; do not rerun its memory
pilots. New withheld inventory: two scenarios, five levels, starts 0/8/32,
lengths 1/4/16 = 90 cases. Exact checkpoint-minus-candidate errors initialize
these blocks; they never reset a time-zero full-tail radius. Generator stage
enclosures are calculated before the withheld exact comparator is consulted.
Any escaping exact state is unsound. Preserve all attempted block outcomes.

If any new short block cannot complete, stop after the short inventory and do
not launch full tails. Per the authorization, disposition is
`stop_inconclusive_or_wrong_parent` for a failed 90-block eligibility control,
with an explicit subclass distinguishing a verified parent from new-method
cell inconclusiveness. Do not call a cell ambiguity an escaped target or a
physical error. If all 90 pass, attempt the ten B96 16-second tails, internal
and boosted levels 0..4, entirely from time zero without later comparator.

Per-process limits: 2 GiB address space, 900 seconds per registered case.
At most four independent case subprocesses. Scientific records omit clock,
RSS and allocator timing; these are separate external receipts. Fixed logical
slot/exponent guards are deterministic. Repeat scientific records byte-for-byte.

## Observers and decisions

For an eligible full run, stream maxima/finals of representation position and
momentum errors using the frozen exact SI budgets. Energy differences use
quadratic kinetic identities and certified potential cells. Stream a signed
weighted energy numerator using exact known full-horizon mean time and outward
endpoint arithmetic. Do not replace signed accumulation by absolute-weight
maxima. Candidate invariants/centrality are measured as exact dyadics, not
declared zero. Frame bounds require both target enclosures. Chord-domain tests
must include candidate chord plus both endpoint error bounds. Inconclusive
state/observer propagation leaves downstream budgets UNCERTIFIED; it is not
permission to infer them from another precision. B128/B192 conditional tests
are ineligible absent a sound complete B96 budget violation.

Mutations cover zero/omitted defects, candidate-as-truth, wrong/assumed force
cells, inward rounding, zero checkpoint reset, dropped subtraction components,
B256 truth, absolute-before-signed slope accumulation, and ambiguity/resource
stops relabeled as passes. Known-answer affine recurrences and independent
exact interval/cell arithmetic precede registered data.

Decision order: wrong parent/failed short eligibility; unsound containment;
full-run force-cell inconclusive; resource inconclusive; proved B96 budget
exceedance; or retain B96 only after all ten full-tail quantities pass.
Every outcome remains NO_PROMOTION. Preserve failures, independent replay,
cross-platform/Python/Lean gates, deterministic evidence, seal and stop.
