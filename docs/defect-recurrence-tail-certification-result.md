# Defect-Recurrence Tail Certification Lab — result

Accepted parent: `171066fc15d6096739c7aeb2e99da7313f447d3f`.
Branch: `defect-recurrence-tail-certification-lab`.

```text
decision: stop_inconclusive_or_wrong_parent
subclass: VERIFIED_PARENT_SHORT_CONTROL_FORCE_CELL_INCONCLUSIVE
selected_precision: null
promotion: NO_PROMOTION TO AUTHORITATIVE WORLD DYNAMICS
```

**The parent is correct. The new 512-bit component-error enclosure fails its
short-block eligibility gate, not containment. No full tails were launched.**
The primary label follows the authorization's first decision rule for a failed
90-block control. Its precise cause is force-cell inconclusiveness, not a wrong
parent, a defective candidate trajectory, or a proved physical-budget violation.

## Mathematics and frozen science

Preregistration `914325e` includes the core Lean proof, compiled before the full
verifier implementation. Five exact lemmas prove the arbitrary-candidate defect
identity, outward enclosure step, stage-sequence induction, signed streaming
accumulator and rounding slack. The affine linear part is modeled as an additive
map over an arbitrary commutative additive group. No theorem assumes B96 is
exact or proves the executable 512-bit arithmetic. The pinned local build
completed 2,142 jobs; reported theorem dependencies are only propext,
Classical.choice and Quot.sound.

The accepted candidate implementation, all physics, force cells, KDK ordering,
unit scales, precision inventory, safe domain and budgets remain untouched.
All inherited source bytes are unchanged except additive theorem reporting.
The accepted lineage was fast-forwarded into main through its existing protected
checks. This new research branch must await review before any further merge.

## Parent and candidate authentication

Both sealed certificate parents are authenticated. The unchanged independent
box control reproduces its first-step dependency stop and 60/90 blocks with
1,320 checked stages. The accepted shared-affine inventory remains 90/90 and
1,890 joint stage checks. Its prior fresh public replay is retained; the new
lab does not blindly repeat its expensive memory-exhaustion pilots.

An additional audit authenticates **1,930 B96 stage wires**, covering all ten
cases through 48 complete steps, against the original bounded-phase sealed
`invariants.csv`. The complete authenticated file and original outer seal are
included. The independent decoder reconstructs dyadic values from wire fields;
it does not use the candidate's dyadic-value decoder. Initial and complete-step
hashes also match the compact accepted exact-prefix corpus during block runs.
The stage-hash audit cannot influence the target force-cell choices.

## Exact stage defects and fixed-size error state

Each conditioned stage constructs its exact rational affine matrix and evaluates
`d = A*candidate - candidateNext` exactly from canonical candidate wire values.
For every output component, exact rational sums enclose `A*error+d`, followed
only by outward 512-significand-bit endpoint rounding. The independent image
checker reconstructs extremizing Cartesian input points to verify the result.

There are 24 current error intervals / 48 endpoints and a 24-by-24 scratch
matrix (576 logical coefficients) for each K4 case. No historical affine
symbols, expression DAG or rounding-provenance cache is retained. Endpoints
are exact Python representations of 512-bit dyadics, with the preregistered
finite verifier exponent guard [-16384,16384]. These are logical-size bounds,
not a claim that Python objects occupy exactly a packed 512-bit payload.
The exact comparator is separate test machinery and never propagates the
certificate. The 2 GiB process limit remains enforced; it was not reached.

## Short-control result

| Starting checkpoint | Blocks completed | Checked stages | Outcome |
| --- | ---: | ---: | --- |
| Exact time zero | 0/30 | 60 | Step 1, second kick: force cell inconclusive |
| Exact step 8 | 30/30 | 630 | Every withheld stage contained |
| Exact step 32 | 30/30 | 630 | Every withheld stage contained |
| Total | 60/90 | 1,320 | Full-tail launch gate closed |

Every failing case names relation 5, x-component. The exact incoming target
relative coordinate is zero and lies inside the generated interval. No checked
target state escapes its enclosure. Independent repeats reproduce **all 90
scientific JSON records byte-for-byte**, including the failure details and
stage-stream hashes. No timing or allocator field enters these records.

The stopping rule prohibits launching the ten full tails after this failed
eligibility control. Full-tail energy, frame, structural-residual and physical
budget certificates therefore remain unimplemented/unexercised beyond the
tested signed-accumulator primitive. No budget is inferred from a shorter run,
an absolute measured candidate residual, or a B256 reference.

## Causal diagnosis

After the generator stops, a separate exact diagnostic checks the two endpoint
errors. In every failed case:

* the candidate relative x-coordinate is exactly zero;
* the exact target relative x-coordinate is exactly zero;
* the two exact endpoint errors are equal;
* their two stored component intervals are equal but have nonzero width.

Independent interval subtraction therefore produces a range strictly straddling
zero instead of preserving the equality of those errors. That range covers
multiple binary64 inputs, so the verifier correctly refuses to select a cell.
The exact equality is reported diagnostically, never injected into propagation
as an unregistered symmetry repair. The same interval is independently checked
against the exact binary64 preimage in the result audit.

The defect recurrence successfully removes the historical-symbol storage
mechanism, but **recentring alone does not retain cross-coordinate dependency**.
Even very narrow component error intervals can lose an exact cancellation.
This is a limitation of this registered enclosure, not evidence that B96
mechanics, its dynamics, or the defect identity is defective.

## Validation, publication, and stop

Fifteen known-answer/mutation controls cover exact/omitted stage defects,
candidate recentering and candidate-as-truth, inward bounds, wire corruption,
B256 substitution, dropped relative-error components, zero checkpoint resets,
uncertified/wrong cells, signed accumulation, fixed exponent limits, and cell/
resource stops relabeled as passes. Failed control outcomes remain preserved.

Publication source, archive digests, CI and fresh-download outcomes are recorded
by the immutable seal and external receipts, not predeclared here. Normal fresh
verification authenticates parent records, reruns the candidate stage-hash and
dependency audits, and replays all 90 NEW short blocks with exact record
comparison. It does not rerun the old 90 shared-affine blocks, full historical
exact prefixes, or memory-exhaustion pilots. `--records-only` explicitly skips
the new 90-block replay.

This checkpoint selects no precision, proves no full-tail budget and establishes
no bounded-state structural defect. Seal this short-control failure and stop;
do not branch the enclosure, increase precision, impose shared-error equality,
change mechanics, or launch a replacement experiment without head-agent review.
