# Post-amendment input verification checkpoint

This is a pre-data progress receipt, **not** a complete input seal, candidate
result, final lab disposition, or publication request. Candidate evaluations:
**zero**. **NO_PROMOTION** remains unchanged.

## Frozen validity amendment

Commit `228e77fc8777deaf893d43f064c5126a7182bc86` changes only the governing
preregistration. It implements the authorized full-facet material-complex
definition and distinguishes invalid within-complex overlap from the registered
moving-pair post-contact penetration. Occupancy remains the complete union;
overlapping volume and buried component facets cannot be counted separately.
The full time-zero-through-two query inventory is retained.

The substantive original overlap audit at
`03e0d3971643541d2b9459ad767df07aec7a2df4` and cleanup at
`1942e62a5d9b18a0b40aa1a110a77a22db9eedf6` remain preserved. The historical
input-gate audit document is not rewritten to erase its original stop.

## Additional checks completed after the amendment

Receipts remain in `build/occupied-geometry-input-attempt-v1/`:

- The exact overlap witness passes with **full triangular facet** connectivity:
  two 64-cell complexes; cells 60 and 83 belong to different complexes. Shared
  edge/vertex adjacency is explicitly tested not to connect complexes.
- Independent direct-binomial weight replay passes: 560 exact monomial checks,
  32,768 independently replayed integrals and 37,448 child allocations.
- All seven finite query inventories pass for all 33 variants, including an
  independently reconstructed exact transform inventory and closed selectors.
- All five paired-sphere input levels pass independent streaming checks of
  translations, primitive identity/reference offsets, incidence, weights,
  material amounts and resolution descriptors.
- All fifteen moving input tables (five pair, five plane and five U levels)
  pass independent per-primitive velocity checks.
- Nine exact quadratic and seven exact plane controls pass; these are oracle
  controls, not extra candidate fixtures or candidate CCD measurements.
- Eleven input unit tests pass. The ordinary pinned Lean build, now importing
  the geometry statements in the axiom report, completes successfully with
  warnings fatal: **2,146 jobs**. This is a local formal build, not new CI.
- Exact global-transform decoding is implemented and tested for geometry,
  sample weights, resolution descriptors and motion. The finest sphere sample
  identity decode reproduces its original 164,926,840 bytes and SHA-256
  `5671207c1f88197c026688de4ac5b9f3bf9ccf75e5a2b8d2be8d2ce8922a822f`.
  The finest cube sample Rz decoded stream has 888,797,208 bytes and SHA-256
  `d323326d1a2ca46e5f0650b70468e98c65db514b2e3e5626e54fcd3cc36a393e`.
  These two stream checks do not certify the complete transform inventory.

## Preserved implementation attempts

The previous slow weight replay and its interruption logs remain available.
Commit `eedf039` regroups an exact binomial sum without changing its value,
quadrature, arithmetic precision, bounds or allocation. A literal ungrouped
formula is independently compared in the unit tests.

Commit `cb5d43e` preserves the first strict selector-check attempt. It incorrectly
compared moving half-space coefficient tuples with fixed-time plane tuples.
Commit `2953e5b` corrects that checker and adds a direct regression of
`d0 + t*d1`. No candidate-visible query bytes were changed. The failed log and
complete corrected seven-fixture replay are both retained.

## Gates still open

No complete candidate/oracle manifest or root digest has been issued. Remaining
input work includes the complete expanded-stream transform/order/relabel hashes,
explicit plane-fixture base translation in the final input encoding, closed
oracle/search obligations and independent checks of the final factored package.
The allowed factoring must preserve every decoded byte and logical count; it
cannot replace the input seal with a generator hash.

The 150 base primitive tables occupy 8,326,002,913 bytes before content
deduplication and 8,326,002,680 bytes after identical-file deduplication. That is
only a storage preflight, not a claim that the full package meets the 8 GiB
ceiling. Motion, queries, oracle witnesses, manifests and new scientific evidence
must also be accounted for. Expanded scratch attempts are preserved separately;
no ceiling is raised and no required logical payload is silently omitted.

The inherited 40-case observer parity gate, candidate measurements, full lab
CI, deterministic scientific twins and final evidence sealing are not yet
claimed. Main and accepted mechanics remain unchanged. No tag or release has
been created.
