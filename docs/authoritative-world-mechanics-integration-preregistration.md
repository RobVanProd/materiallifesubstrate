# Authoritative World Mechanics Integration Lab — preregistration

Parent: `e2f37a6f5d48432eb909b0a42948868c9771b036`.
Branch: `authoritative-world-mechanics-integration-lab`.
Accepted parent tag: `authoritative-mechanics-kernel-parity-lab-evidence-v1`.
Parent archive: 817038443 bytes; SHA-256
`89558130222a5cf33353b01e71f06cf93437ab446652cbea93577c1d6f7e75e2`.
Parent manifest: `fc81fd9d1c373eeb9775394b4864405a2ae0b8a6202df671000cf00d5a08115f`.

**NO_PROMOTION TO AUTHORITATIVE WORLD DYNAMICS.** Commit and push this plan
after accepted-parent publication/merge and before implementation or trajectory
evaluation. This is integration verification, not a new numerical experiment.

## Frozen mechanics and decisive comparison

The accepted kernel source/header, Python reference, B96 RN-even primitive
boundaries/exponent range/wire, Path-B binary64 operation order, force/energy
graph, H_force, units, topology/reference state, KDK, event construction,
domain scratch accounting and r/l0 >= 2^-24 complete-chord predicate remain
unchanged. No alternative arithmetic, force correction, hidden remainder,
substep, cap, clamp or projection. The inherited first-order control remains
permanently ineligible and is replayed only because it belongs to the frozen
30-case short inventory.

The decisive comparison is the complete World-driven scientific byte stream
against the authenticated isolated C++ stream, not only endpoints or a
tolerance comparison. World may receive model inputs and initial/checkpoint
state, never expected next states or force/event values. Every mechanics step
calls the unchanged accepted kernel in-process; no Python subprocess may
participate in a World transition.

## Quarantined architecture

An explicit build option, OFF by default, builds a separately named research
World library/executable. The ordinary mls_core target and normal headless
application remain unactivated. Conditional World hooks compile only in the
research target. An additional explicit runtime enable is mandatory; a missing
enable must fail closed rather than implicitly activating research mechanics.

World owns one complete canonical B96 mechanics state and immutable registered
model/schedule metadata. No conversion into the legacy integer PacketStore is
allowed. Legacy packets, compounds, grid, ledger and energy reservoirs are
unrelated sentinel state in this lab, not a second representation of the B96
packets. Mechanics IDs remain exactly the kernel IDs; the research namespace
is explicit and must not alias legacy packet handles through an implicit map.

World::step schedules the research transition when enabled and attached. It
must not also execute legacy ballistic advancement or any other force map.
The SI clock uses the inherited 1/1,000,000,000 second raw time quantum.
Registered positive even dt and step index/time consistency are checked.
Successful commit updates World clock and the kernel state together. Failure
leaves all prior World state, clocks and committed observer output unchanged.
For a multi-step World call, preserve the existing all-or-nothing transaction
semantics; single-step calls supply the frozen scientific inventory.

The legacy checkpoint/accounting interfaces must not silently omit attached
research mechanics or label bounded residuals exact conservation. A separate,
versioned research checkpoint contains the complete legacy checkpoint plus
canonical mechanics bytes, model, schedule/clock identity and any retained
observer snapshot. It must round-trip canonically, reject corruption/truncation,
and restore in a fresh process without a reference trace. Legacy checkpoint
format/physics ABI and default World behavior remain unchanged when research
is disabled. Physical-state hashing must include attached research mechanics;
observer reads return copies and cannot influence continuation.

## Frozen inventory and hard gates

Authenticate the parent archive/manifest and exact frozen kernel before data.
Replay all 30 short cases and all ten 16-second tails: 2976 short steps and
15872 long steps, including 47616 long KDK stages. Compare every state/stage
byte, Path-B input/output bit and scientific event line/hash. No shorter or
easier replacement and no B256 truth trace.

For every primary trajectory require deterministic full twins, an interior
World checkpoint, separate-process restore and complete subsequent scientific
suffix identity. At each committed step check World clock, mechanics clock,
packet IDs/masses and unchanged unrelated legacy state. Mutating a returned
observer copy or changing observer read frequency must not change physics.

World-specific controls cover runtime/build disabled behavior, enabled but
unattached rejection, zero-step no-op, atomic domain/range failure, clock
rollback, checkpoint save/restore/corruption, ID stability, no double kick or
legacy drift, observer isolation, unrelated ledger/material/grid/history
preservation, and nonzero initial/checkpoint time. Do not reinterpret
approximate bounded-state reversal/conservation as exact invariants.

Run the ordinary core validation suite with the default target and with the
research-capable target but runtime disabled. Require GCC/Clang/MSVC complete
stream parity, inherited Python exact oracle and pinned Lean/trust gates.
No new formal claim about C++ is made; the inherited mathematical results do
not prove this executable transaction implementation.

Keep the inherited 2 GiB process backstop. Resource exhaustion or incomplete
coverage is inconclusive, not parity. Test independent comparison/inventory
mutations and compiled World-boundary mutations covering omitted/double
transition, accidental legacy drift, premature clock mutation, failed-step
commit, checkpoint omission, changed packet identity and observer feedback.
Mutations must be rejected, not included as eligible implementations.

## Evidence and decision order

Wrong source, parent identity or reference inventory:
`stop_inconclusive_or_wrong_parent`.
First mechanics byte/bit/event mismatch:
`stop_world_kernel_semantic_divergence`.
Atomicity, ownership, checkpoint, clock, isolation or default-path failure:
`reject_world_mechanics_integration_contract`.
Incomplete/resource/platform verification:
`stop_world_mechanics_integration_inconclusive`.
All registered gates:
`retain_cpp_kernel_world_integration_for_research`.
Every disposition remains NO_PROMOTION; no tolerance-based parity passes.

Preserve first divergences and failed source/executable/records before fixing
an implementation defect. Do not alter frozen inputs or gates to obtain a
pass. Seal exact source, inputs, full traces, controls/mutations, platform CI
and deterministic twin archives; perform a fresh-extraction executable replay.
Stop with the local sealed result for independent review. This mandate does
not publish/merge the new integration lab or authorize contact, fracture,
topology evolution, chemistry changes, optimization, GPU or production use.
