# Relation-Coordinate Defect Tail Certification Lab — result

Decision: `retain_b96_bounded_phase_state_for_research`.
Selected precision: **96**.
Promotion: **NO_PROMOTION TO AUTHORITATIVE WORLD DYNAMICS**.

Parent: `99d979334f0b931401d451beba70029d6bca52d7`, accepted annotated tag
`defect-recurrence-tail-certification-lab-evidence-v1`.
The parent, all earlier scientific dispositions, source, tags and releases
remain unchanged. Main was fast-forwarded to that exact accepted parent;
this result belongs to a separate research branch.

## Certified claim

The frozen B96 candidate's ten registered 16-second internal/boosted K4
trajectories satisfy the unchanged physical representation-error and
structural-residual budgets, using direct relation-coordinate defect
enclosures against the exact-rational KDK map. The target includes the
accepted binary64 force evaluator. It is NOT a smooth ODE substitute and
B256 is never used as truth.

This is a finite-inventory, finite-horizon certificate. It is not a proof of
arbitrary materials, indefinite time, evolutionary safety, exact bounded-state
conservation, bit-exact signed-time reversal, or production readiness.
The parent's failed exact-reversal results are not relabeled as passes.

## Controls and causal result

The accepted parent identities and authenticated evidence reproduce the old
independent-box controls: 60/90 blocks, 1,320 stages, including the immediate
time-zero dependency failure. The accepted shared-affine 90/90 record and
1,890 checks remain authenticated; its resource-exhaustion pilots were not
rerun. The parent verification also independently authenticates 1,930 B96
stage wires and the ten prior endpoint-equality diagnostics.

The NEW direct relation-coordinate method passes **90/90** registered short
blocks and **1,890 joint stage checks** of exact packet AND exact relation
position/velocity containment. All thirty time-zero blocks pass the first
step's second kick. The independent repeat is byte-identical.

Ten matched new diagnostics reconstruct the old failure without consulting
the exact target after initialization. Direct relation 5 x-error remains
exactly `[0,0]` through its own kick/drift recurrence. The corresponding
independent packet-error subtraction straddles zero and cannot certify the
cell. No post-hoc endpoint equality narrows an enclosure.

The complete inventory certifies **15,872 steps**, **47,616 stages** and
authenticates **63,498 canonical candidate wire states** against the original
sealed B96 trajectory hashes. Every force cell and every target drift chord
is certified; no ambiguity or resource limit fires. All ten full scientific
records, frame streams and receipts reproduce byte-for-byte in the independent
repeat. No exact-rational trajectory is read after time-zero initialization
in these full continuations.

## Physical budgets

Displayed numbers are rounded summaries. The auditor compares exact rational
values and frozen budgets; there is no adjacent-precision ratio gate.

| Quantity, maximum over registered tails | Certified bound or exact candidate residual | Frozen budget |
| --- | ---: | ---: |
| Position representation error (m) | 4.90376203e-28 | 7.45058060e-18 |
| Momentum representation error (kg m/s) | 2.66083769e-29 | 1.42108547e-14 |
| Representation-energy error (J) | 3.20207143e-31 | 1.11022302e-16 |
| Representation-energy slope magnitude (J/s) | 1.68592013e-32 | 6.93889390e-18 |
| Candidate total-momentum residual (kg m/s) | 1.94256998e-29 | 1.42108547e-14 |
| Candidate orbital-angular-momentum residual (kg m²/s) | 3.52521410e-29 | 1.11022302e-25 |
| Candidate relation-centrality residual (kg m²/s) | 1.96707430e-31 | 1.11022302e-25 |

Final position, momentum and energy bounds are separately exported and checked
against the maxima. Candidate momentum/angular residuals are computed directly
from exact stored dyadics at every stage. Centrality covers the rounded
relation impulse and both actual endpoint impulses, not merely their nominal
central direction. These residuals are bounded, not declared zero.

All five paired levels pass the parent's literal first-packet-relative
position/momentum budgets. Additional COM-relative controls also pass.
Both nominal candidate differences and rigorous exact-target differences
are checked using relation-derived enclosures. The conversion from COM
momentum to the inherited first-packet observable is restricted explicitly
to this frozen equal-mass K4 inventory.

Potential uncertainty uses certified direct relation cells. Kinetic differences
use exact quadratic identities in the packet momentum error. Energy slopes
use a streaming SIGNED least-squares numerator over all samples including
time zero, with mean time 8 seconds. Raw enclosures use target-minus-B96, as
does the state defect recurrence; negate both endpoints in reverse order for
the parent's B96-minus-target sign convention. Absolute budgets are unchanged.
No numerical discrepancy enters a physical energy reservoir.

## Implementation and verification scope

There are 24 packet-error intervals and 36 direct relation position/velocity
error intervals. The two stage matrices have 1,872 logical coefficient slots.
Numerical history is reduced to current enclosures; there are no historical
noise symbols, DAGs or provenance caches in causal verification state.
All propagated endpoints use the preregistered 512-bit outward dyadic format.
Exact stage-local rational work is discarded after propagation. Streaming
evidence files grow with time; active numerical storage does not. The frozen
2 GiB per-process limit remains in force.

The per-run engine deliberately labels its output
`state_horizon_enclosed_observers_pending`: it supplies state/local-observer
enclosures but does not make the cross-trajectory inventory decision. The
independent auditor checks the complete inventory, signed slope, exact
residuals, frozen and COM frame definitions, source authentication and twins.
Its `evidence/result.json` is the full-budget decision, not a relabeling of
an unresolved generator run.

Fourteen new known-answer/mutation tests and fifteen inherited defect tests
pass. New tests include incidence closure for every basis vector with unequal
masses, common-mode annihilation, arbitrary-center defects, direct-zero cell
control, orientation/mass mutations, inward/omitted-error rejection, exact
rational energy containment, independent COM arithmetic, and false-launch
mutations. Every generated interval image is independently checked with exact
extremizing-point arithmetic. The short exact comparator is withheld until
after enclosure generation.

Ten new exact Lean theorems cover incidence/common modes, conditioned kick and
drift closure, observable errors, induction, cell membership, rational time
scaling, COM observables and budget inclusion. The local pinned build passes
2,143 jobs with `--wfail`. No claim is made that Lean verifies the executable
512-bit rounder or binary64 implementation. The C++ regression passes, and
the final source/tag CI receipts are publication requirements recorded
separately, not inferred from earlier commits.

## Preserved trials and publication

Two unsealed implementation inventories are preserved: phase/energy
continuation, then supplementary observer reporting. Their numerical stage
hashes and position/momentum/energy enclosures equal the final inventory.
They were not used to relax a criterion or change the recurrence; the final
complete observer schema was replayed independently before this decision.
Neither implementation inventory is presented as a failed mechanics result.

The bundle includes frozen source, nested authenticated parents, the 90-block
twins, complete ten-tail twins and streaming frame evidence, matched controls,
exact audit, logs and preserved implementation records. The default fresh
verifier replays all NEW short blocks AND all ten complete tails and compares
their scientific files. `--records-only` explicitly omits those new replays.
Neither mode claims the optional old full exact-prefix replay or old memory
pilots. Publication acceptance requires final-source CI, immutable-tag CI and
fresh public-download size/hash plus bundled-verifier success.

**Stop at the research certificate. No authoritative World mechanics is enabled.**
